// Filter sheet is no longer used — filtering is now done through the
// cascading City → Phase → Sector → Category selector on the home screen.
// This file is kept as a placeholder to avoid import errors in case
// any screen still references it.
